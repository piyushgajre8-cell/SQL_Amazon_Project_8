# SQL_Amazon_Project_8
This SQL file creates a database named shop_data with two tables — customers and vendors. It stores their names and cities, then combines both tables using the UNION command to show all unique cities where customers and vendors are located.
